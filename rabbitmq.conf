# RabbitMQ Configuration for Fly.io
# https://www.rabbitmq.com/configure.html

# Networking
listeners.tcp.default = 0.0.0.0:5672

# Management plugin
management.tcp.port = 15672
management.tcp.ip = 0.0.0.0

# Security
default_user = admin
default_pass = !@#Solenge123!

# Memory and disk limits (relaxed for development)
vm_memory_high_watermark.relative = 0.9
disk_free_limit.absolute = 100MB

# Logging
log.console = true
log.console.level = warning

# Clustering (disabled for single instance)
cluster_formation.peer_discovery_backend = classic_config

# Load definitions on startup
management.load_definitions = /etc/rabbitmq/definitions.json

# Heartbeat settings for better connection reliability
heartbeat = 60

# Connection limits
connection_max = 1000